/**
 * @module external
 * @layer L2 — extensions barrel export
 *
 * External event sources: HTTP webhook listener, file system watcher,
 * and the pluggable normalizer registry.
 */

export * from './http-listener.js';
export * from './file-watcher.js';
export * from './normalizer.js';
